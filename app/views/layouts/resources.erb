<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
  "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">

<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
  <head>
    <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
    <title>( Michael )</title>
    <%= stylesheet_link_tag 'default' %>
  </head>
  <body>
    <%= render :partial => "shared/header",:object => @user %>
    <div id="main">
      <div id="content">
                 <%   if flash[:notice].present?%>  <p class="notice"><%=flash[:notice] %></p>  <% end %>
            <%   if flash[:error].present?%> <p class="error"> <%=flash[:error]  %></p> <% end %>
        <div id="sidebar">
          <div id="sidebar_top"></div>
          <div id="sidebar_text">
            <h2><%= link_to_unless_current "#{@user.nick_name}关注的人（#{@user.followings.size}）" ,user_follows_path(@user)%></h2>
            <h2><%= link_to_unless_current "关注#{@user.nick_name}的人（#{@user.followers.size}）",user_be_follows_path(@user) %></h2>
            <h2>
              个人信息： <%=link_to "编辑",edit_user_path(current_user) if current_user==@user %></h2>
           
            <p><%= render :partial => "shared/user_profile_full",:object => @user %></p>
            <h2>
              <%= calling(@user) %>
              擅长的：</h2>
            <div>
              <% for k in @user.appear_keyword_pages %>
                <p>
                  <span class="keyword_page"><%=link_to k.keyword,keyword_page_path(KeywordPage.find_by_keyword(k.keyword)) %></span>
                  <cite>生命值：<strong><%=number_with_precision k.field_value(@user) %></strong>,排名：<%= order(@user,k) %>
                  </cite>
                </p>
              <% end %>
            </div>
          </div>
          <div id="sidebar_bottom"></div>
        </div>
        <div id="text">
          <%= @content_for_layout %>
        </div>
      </div>
      <%= render :partial => "shared/footer" %>
    </div>
  </body>
</html>
