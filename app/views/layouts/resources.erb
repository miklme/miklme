<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
  "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">

<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
  <head>
    <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
    <title>Resources: <%= controller.action_name %></title>
    <%= stylesheet_link_tag 'default' %>
  </head>
  <body>
    <%= render :partial => "shared/header",:object => @user %>
    <div id="main">
      <div id="content">
        <% if flash[:notice] or flash[:error] %>
          <div id="notice">
            <%= flash[:error] or flash[:notice]%>
          </div>
        <% end %>
        <div id="sidebar">
          <div id="sidebar_top"></div>
          <div id="sidebar_text">
            <h2>
              <% if not @user==current_user %>
                <% if @user.sex==1 %>
                  他
                <% elsif @user.sex==0%>
                  她
                <% end %>
              <% else %>
                我
              <% end %>
              的个人信息：</h2>
            <p><%= render :partial => "shared/user_profile_full",:object => @user %></p>
            <div>
              <% for k in @user.appear_keyword_pages %>
                <p>
                  领域：<%=link_to k.keyword,keyword_page_path(KeywordPage.find_by_keyword(k.keyword)) %>
                  <!--
                  隐藏领域的功能，暂时不打算使用
                  <% if @user.value_orders.find_by_keyword_page_id(k).hidden==false%>
                    <strong><%=link_to k.keyword,keyword_page_path(k) %></strong><span class="little_white"><%= link_to "  隐藏  ",:controller => :keyword_pages,:action => :hide_field,:id => k%></span>
                  <% else %>
                    <%= k.keyword %> <strong>(已隐藏)</strong><span class="little_white"><%= link_to "恢复",:controller => :keyword_pages,:action => :appear_field,:id => k %></span>
                  <% end %>
                  -->
                  <cite>价值点数：<strong><%= k.field_value(@user) %></strong>,排名：(<span class="highlight"><%= ValueOrder.order(@user,KeywordPage.find_by_keyword(k.keyword)) %></span>/<%= k.users.size %>)
                  </cite>
                </p>
              <% end %>
            </div>
            <h2><%= link_to "查找用户>>>",search_user_new_user_follow_path(@user) %></h2>
          </div>
          <div id="sidebar_bottom"></div>
        </div>
        <div id="text">
          <%= @content_for_layout %>
        </div>
      </div>
      <%= render :partial => "shared/footer" %>
    </div>
  </body>
</html>
