<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
  "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">

<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
  <head>
    <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
    <meta content="Mikl,Michael,Mikl.me,Michael网络,话题,声望,在线声望" name="keywords"/>
    <title>( Michael )</title>
    <%= javascript_include_tag :defaults,:cache => true  %>
    <%= stylesheet_link_tag "result"  %>
  </head>
  <body>
    <div style="background-image: url(/images/j2.jpg); opacity: 1" id="sw_im">
    </div>
    <div id="sw_page">
      <div id="sw_hdr">
        <div class="sw_tb">
          <% if logged_in? %>
            <%= render :partial => "shared/keyword_page_header",:object => current_user %>
          <% else %>
            <%= render :partial => "shared/keyword_page_header"%>
          <% end %>
        </div>
      </div>
      <div id="sw_content">
        <div id="sw_canvas">
          <%= render :partial => "keyword_pages/keyword_page_side" %>
          <div id="content">
            <%   if flash[:notice].present?%>  <p class="notice"><%=flash[:notice] %></p>  <% end %>
            <%   if flash[:error].present?%> <p class="error"> <%=flash[:error]  %></p> <% end %>
            <%= render :partial => "shared/search_bar" %>
            <% if Resource.find_by_keyword_page_id(@keyword_page).blank?%>
              <div class="item_blue">
                <span class="cadet_blue">目前这个话题内还没有任何发言。幸运的是，如果你发言，你就有<cite>1点</cite>初始声望。</span>
              </div>
            <% end %>
            <% if logged_in? and @keyword_page.users.include?(current_user) %>
              <div class="item_blue">
                每多1个人关注你，你在<span id="keyword_page">这个话题</span>的<cite>声望</cite>会提高1点。要不要邀请朋友关注自己呢？
                <ul>
                  <li>
                    <p>我终于发现了一个独特的应用，你应该对此感到好奇。注册在这里，注册后你就自动关注我了：<%= "http://mikl.me/users/new/#{current_user.id}/#{ValueOrder.find_by_user_id_and_keyword_page_id(current_user.id,@keyword_page.id).id}" %></p>
                  </li>
                </ul>
              </div>
              <div class="item_orange">
                关于<span id="current_page"><%=@keyword_page.keyword  %></span>，有什么想要告诉大家的？
              </div>

            <% end %>

            <!--
            有趣的分割点
            <span class="sc_bullet">·</span>%>
            -->

            <%= @content_for_layout %>
          </div></div></div></div>
    <%= render :partial => "shared/footer" %>
  </body>
</html>
