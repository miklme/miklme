<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
  "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">

<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
  <head>
    <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
    <meta content="Mikl,Michael,Mikl.me,Michael网络,世界,生命值,在线生命值" name="keywords"/>
    <title>( Michael )</title>
    <%= javascript_include_tag :defaults,:cache => true  %>
    <%= stylesheet_link_tag "result"  %>
  </head>
  <body>
    <div id="sw_page">
      <div id="sw_hdr">
        <div class="sw_tb">
          <% if logged_in? %>
            <%= render :partial => "shared/keyword_page_header",:object => current_user %>
          <% else %>
            <%= render :partial => "shared/keyword_page_header"%>
          <% end %>
        </div>
      </div>
      <div id="sw_content">
          <%= render :partial => "keyword_pages/keyword_page_side" %>
          <div id="content">
            <%   if flash[:notice].present?%>  <p class="notice"><%=flash[:notice] %></p>  <% end %>
            <%   if flash[:error].present?%> <p class="error"> <%=flash[:error]  %></p> <% end %>
            <%= render :partial => "shared/search_bar" %>
            <% if Resource.find_by_keyword_page_id(@keyword_page).blank?%>
              <div class="item_blue">
                <span class="cadet_blue">天堂与地狱贴在了一起，这个世界还没有人。</span>
              </div>
            <% end %>
            <% if logged_in? and @keyword_page.users.include?(current_user) %>
              <div class="item_blue">
                每邀请1个朋友关注你，你<cite>生命值</cite>会提高1点。要不要邀请呢？
                <ul>
                  <li>
                    <p>我终于发现了一个独特的应用，你应该对此感到好奇。注册在这里，注册后你就自动关注我了：<%= "http://mikl.me/users/new/#{current_user.id}/" %></p>
                  </li>
                </ul>
              </div>
              <div class="item_orange">
                对于<span id="current_page"><%=@keyword_page.keyword  %></span>，你有着怎样的观点，也即<span id="current_page">世界观</span>：
              </div>

            <% end %>

            <!--
            有趣的分割点
            <span class="sc_bullet">·</span>%>
            -->

            <%= @content_for_layout %>
          </div></div></div>
    <%= render :partial => "shared/footer" %>
  </body>
</html>
