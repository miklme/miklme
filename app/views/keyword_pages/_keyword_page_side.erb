<div id="sw_aside">
  <div id="sc_expPane">
    <div class="sw_menu" id="sw_rel">
      <dl>
        <dt>
          当前世界
        </dt>
        <dd>
          <span id="current_page"><%=h @keyword_page.keyword %></span>
        </dd>
      </dl>
      <h2>相关世界</h2>
      <!--暂时取消用户自定义的相关世界
      <% if logged_in? and current_user.controlled_keywords.include?(@keyword_page.keyword).present? %>
              <span><%=link_to "修改",keyword_page_related_keywords_path(@keyword_page),:popup => ['修改推广世界', 'height=500,width=450'] %>
              </span>
      <% end %>
      -->
      <ul>
        <% if not @searched_keywords.blank? %>
          <% for keyword in @searched_keywords %>
            <li id="keyword_page">
              <span><%= link_to keyword.keyword,keyword_page_path(keyword)%></span>
            </li>
          <% end %>
        <% end %>
      </ul>
      <% if logged_in? %>
        <h2>
          最近回应我的
        </h2>
        <%= render :partial => "news/keyword_page_news",:collection => @news %>
      <% end %>
    </div>
  </div>
</div>