
<% r=keyword_page_index.resources.good_resource %>

  <h2 class="item_orange">
    关键字：<span id="keyword_page"><%= link_to_page(keyword_page_index) %> · · · · · ·</span>
  </h2>
<% if r.present? %>
  <ul class="sb_results" id="wg0">
    <li>
      <span class="left"><%= render :partial => "shared/field_profile",:locals => {:user => keyword_page_index.resources.good_resource.first.owner,:keyword_page => keyword_page_index}%>
      </span>
      <%= render "resources/resource_left",:object => keyword_page_index.resources.good_resource.first %>
    </li>
  </ul>
<% end %>