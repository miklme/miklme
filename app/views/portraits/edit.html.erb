<% form_for user_portrait_path(@user,@portrait),:url => { :action => "create" }, :html => { :multipart => true } do |f| %>
  <p>上传一个图片，作为头像:
  <%= f.file_field :image_file %>
  </p>
  <p>或者提供一个图片链接:
  <%= f.text_field :image_file_url %></p>
  <p><%= f.hidden_field :image_file_temp %></p>
  <p>
    <%= f.submit "就这样",:onclick => "this.disabled = true" %>
  </p>
  <p>
    <b>图片预览：</b>
    <%= embedded_image_tag(@portrait.operate { |img| img.resize 200 }) if @portrait.has_image? %>
  </p>

<% end %>
