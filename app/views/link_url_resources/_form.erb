<li>
  <% form_for [@user,@link_url_resource] do |f|%>
    <strong><%= f.error_messages %>
    </strong>
    <table cellspacing="5">
      <tr><td><%= f.label :link_url,"链接地址*" %></td><td><%= f.text_field :link_url %></td></tr>
      <tr><td><%= f.label :description,"一句简单的描述，也就是下图中蓝字部分的内容*：" %><%= image_tag "链接描述.jpg",:size => "331x78" %></td><td><%= f.text_field :description%></td></tr>
      <tr>

        <td>你想在哪个领域内的人们看到这个内容*</td>
        <td>
          <% for k in @ks %>
            <cite>
              <%= f.radio_button :keywords,k.keyword %><%= k.keyword %>
            </cite>
          <% end %>
          <p>
          <%= link_to "管理我的领域",edit_user_path(@user) %>
          </p>
        </td>
      </tr>
      <tr><td><%= f.check_box :authority,{},true,false %><%= f.label :authority,"链接内容是我的原创作品，如论坛发表的文字，原创视频、照片作品，原创音乐等。（原创作品可以获得更高的价值点数评分）" %></td>
    </table>
    <p><%= f.submit "完成",:onclick => "this.disabled = true" %> </p>
  <% end %>
</li>