<h1>Listing follows</h1>

<table>
  <tr>
  </tr>

<% @follows.each do |follows| %>
  <tr>
    <td><%= link_to 'Show', follows %></td>
    <td><%= link_to 'Edit', edit_follows_path(follows) %></td>
    <td><%= link_to 'Destroy', follows, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New follows', new_follows_path %>