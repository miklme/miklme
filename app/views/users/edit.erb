<%@page_title='1'%>

<body>
  <div id="header">
    <div id="logo"> <div id="login"></div>
    <%=link_to '1',new_session_path%> </div>
    <%if logged_in? -%>
      <%= link_to '登出', logout_path %>
    <%end%>
    <!--
   <div id="menu">
      <ul>
        <li><a href="#">home</a></li>
        <li><a href="#">about us</a></li>
        <li><a href="#">services</a></li>
        <li><a href="#">pricing</a></li>
        <li><a href="#">contacts</a></li>
      </ul>
    </div>
    -->
  </div>
  <!--end header -->
  <!-- main -->
  <div id="main">
    <div id="content">
      <%if flash[:notice]%>
        <p><%=flash[:notice]%></p>
      <%end%>
      <%if flash[:error]%>
        <p><%=flash[:error]%></p>
      <%end%>
      <div id="text">
        <h3>请先在这里完善信息，共分三步，这里是第一步。 </h3>
        <% form_for @user do |f|%>
          <%= f.error_messages %>
          <p><%= f.label :nick_name,'名(必填)'%>
            <%= f.text_field :nick_name %>
            任何人都能看到，请随意填写。此外，你将拥有这样一个个人地址：mikl.me/你的名号。
            名号当然可以是中文（当你和别人的名号重复时，会员用户理应得到该地址）</p>
          <p><%= f. label :name,'真实姓名'%>
            <%= f.text_field :name %>
            请填写真实姓名(仅好友可见)，通过真实姓名，别人会更容易的找到你。</p>
          <%= f.submit '就这样，下一步'%>
        <% end %>
      </div>
    </div>
    <!-- footer -->
    <%=render :partial=>'shared/footer'%>
    <!-- end footer -->
  </div>
  <!-- end main -->

</body>