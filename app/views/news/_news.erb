<% if news.news_type=="comment" %>
  <p>
    <%= link_to news.owner.name_or_nick_name(current_user),user_path(news.owner) %>的分享
    <%=link_to news.resource.description,news.resource.link_url,:popup => true %>（关键字为 <%=link_to news.resource.keywords,keyword_page_path(via_keyword_to_keyword_page(news.resource.keywords)) %>）被评论了
    <%=link_to news.resource.comments.count,user_link_url_resource_comments_path(news.owner,news.resource),:popup => true %>次，
    <% a=news.resource.comments.map do |c| %>
      <% c.rating %>
    <% end %>
    <% if a.sum.to_i>=0 %>
      价值点数+ <%= a.sum.to_i %>
    <% else %>
      价值点数减少了 <%= -a.sum.to_i %>(反响恶劣)
    <% end %>
  </p>
<% elsif news.news_type=="twitter_resource" %>
  <p>
    <%=link_to news.owner.name_or_nick_name(current_user),user_path(news.owner) %>说：
    <cite><%=link_to news.resource.keywords,user_twitter_resource_path(news.owner,news.resource) %></cite>
  </p>
<% elsif news.news_type=="link_url_resource" %>
  <p>
    <%= link_to "#{news.owner.name_or_nick_name(current_user)}",user_path(news.resource.owner) -%>分享了
    <% if news.resource.authority? %>
      <%= "原创" -%>
    <% end %>
    <% if news.resource.form=="视频" -%>
      <%="视频" -%>
    <% elsif news.resource.form=="音频" -%>
      <%= "音乐" %>
    <% elsif news.resource.form=="图片" %>
      <%="图片" %>
    <% elsif news.resource.form=="文字、文档" %>
      <%="文章" %>
    <% end %>
    <%= link_to "#{news.resource.description}",news.resource.link_url,:popup => true %>
    （关键字为 <%=link_to news.resource.keywords,keyword_page_path(via_keyword_to_keyword_page(news.resource.keywords)) %>）
  </p>
<% elsif news.news_type=="stranger_follow" %>
  <p>
    <%=link_to news.owner.name_or_nick_name(current_user),user_path(news.owner) %>被 <%=link_to news.follower.name_or_nick_name(current_user),user_path(news.follower) %>关注，TA的价值点数+3。
  </p>
<% elsif news.news_type=="friend_follow" %>
  <p>
    <%=link_to news.owner.name_or_nick_name(current_user),user_path(news.owner) %>被 <%=link_to news.follower.name_or_nick_name(current_user),user_path(news.follower) %>加为朋友，TA的价值点数+3.
  </p>
<% elsif news.news_type=="twitter_resource_reply" %>
  <p>
    <%=link_to news.owner.name_or_nick_name(current_user),user_path(news.owner) %>的话
    <%=link_to news.resource.content,user_twitter_resource_path(news.owner,news.resource) %>被回复了
    <%= news.resource.replies.count %>次,价值点数＋ <%= news.resource.replies.count*0.2 %>
  </p>
<%end  %>
