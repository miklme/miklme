<h1>Editing resource</h1>

<% form_for(@user,@resource) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', user_resource_path(@user,@resource) %> |
<%= link_to 'Back', user_resources_path %>